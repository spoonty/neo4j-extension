import { FC } from 'react'
import * as SeparatorPrimitive from '@radix-ui/react-separator'

const Separator: FC<SeparatorPrimitive.SeparatorProps> = (props) => (
  <SeparatorPrimitive.Root {...props} />
)

export default Separator
